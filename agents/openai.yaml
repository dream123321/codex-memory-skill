interface:
  display_name: "Project Memory"
  short_description: "Maintain memory files with daily sync prompts"
  default_prompt: "Use $project-memory-md to load project memory, ask for sync when the daily reminder is due, and keep only correct key facts."
